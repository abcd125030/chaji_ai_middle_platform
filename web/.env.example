# =================================
# Next.js 环境变量配置模板
# =================================
# 将此文件复制为 .env.local 并根据环境修改配置
# 注意：所有 NEXT_PUBLIC_ 前缀的变量会自动暴露给客户端

# ========================
# 基础配置
# ========================
# 环境标识 (development|test|production)
NEXT_PUBLIC_ENV=development
NODE_ENV=development

# ========================
# API 配置
# ========================
# 后端 API 基础地址
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:6066/api

# ========================
# 认证配置 飞书认证的配置由 backend 管理
# ========================
# 启用的登录方式 (逗号分隔，可选值: feishu,google,email)
# 示例: "feishu,google" 或 "google" 或 "feishu,google,email"
NEXT_PUBLIC_ENABLED_AUTH_METHODS=feishu

# 在向 backend 的 django 项目请求时，指定飞书登录重定向 URL
NEXT_PUBLIC_FEISHU_REDIRECT_URI=http://127.0.0.1:3000/_studio/auth/login/callback
NEXT_PUBLIC_CALLBACK_URL=http://127.0.0.1:3000/_studio/

# Google OAuth 配置
NEXT_PUBLIC_GOOGLE_REDIRECT_URI=http://127.0.0.1:3000/_studio/auth/google/callback

# ========================
# 应用配置
# ========================
# 支付回调基础URL (第三方支付平台回调使用，需在支付平台登记)
PAYMENT_CALLBACK_BASE_URL=https://your-domain.com
# 用户可见基础URL (用于页面跳转，用户实际访问的域名)
USER_FACING_BASE_URL=http://127.0.0.1:3000

# The base path for the application.
# Use an empty string for root (e.g., NEXT_PUBLIC_BASE_PATH=)
# Use a sub-directory for hosting in a sub-path (e.g., NEXT_PUBLIC_BASE_PATH=/_studio)
NEXT_PUBLIC_BASE_URL=http://127.0.0.1:3000
NEXT_PUBLIC_BASE_PATH=/_studio

# 内部 API Token，用于后端回调验证
INTERNAL_API_TOKEN=default-internal-token-change-in-production

# ========================
# Redis 配置 (SSE 跨进程通信)
# ========================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_SSE_DB=3

# ========================
# 站点名称配置
# ========================
# 站点简称，用于顶部导航栏等紧凑位置
NEXT_PUBLIC_SITE_NAME="Your Site Name"
# 站点全称，用于登录页、法律文档等正式场合
NEXT_PUBLIC_SITE_FULL_NAME="Your Site Full Name"
# AI助手名称，用于聊天界面
NEXT_PUBLIC_ASSISTANT_NAME="AI Assistant"
# 浏览器标签页标题
NEXT_PUBLIC_BROWSER_TITLE="Your Site Title"

# 首页类型 (enterprise|public)
NEXT_PUBLIC_HOMEPAGE_TYPE=public
# CHAT页面的LOADING类型 (frago|chagee)
NEXT_PUBLIC_LOADING_SVG=frago

# ========================
# 支付配置
# ========================
# 是否启用自定义充值金额功能
# 设置为 true 显示自定义金额输入框，false 或不设置则隐藏
NEXT_PUBLIC_ENABLE_CUSTOM_AMOUNT=false

# 订阅计划定价配置
# 原价（人民币）
NEXT_PUBLIC_PRICE_BASIC_ORIGINAL=93
NEXT_PUBLIC_PRICE_PRO_ORIGINAL=190
NEXT_PUBLIC_PRICE_MAX_ORIGINAL=969

# 折扣配置（百分比，85表示85折，即85%的价格）
NEXT_PUBLIC_PRICE_BASIC_DISCOUNT=85
NEXT_PUBLIC_PRICE_PRO_DISCOUNT=80
NEXT_PUBLIC_PRICE_MAX_DISCOUNT=75

# ========================
# 菜单配置
# ========================
# TopBar 菜单项显示控制（通用页面使用）
# 格式：逗号分隔的菜单项名称，空值表示隐藏全部
# 可选项：agentic, presentation, tools, docs, dashboard
NEXT_PUBLIC_TOPBAR_MENU_SHOW=tools

# ChatTopBar 菜单项显示控制（聊天页面使用）
# 格式：逗号分隔的菜单项名称，空值表示隐藏全部
# 可选项：create, history, payments (payments控制Upgrade按钮显示)
NEXT_PUBLIC_CHAT_MENU_SHOW=create,history

# 前台页面控制注册时是否必须提供邀请码
NEXT_PUBLIC_REQUIRE_INVITATION_CODE=false